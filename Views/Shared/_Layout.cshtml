<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Планирование бюджета</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="~/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    <!-- bootstrap5 dataTables css cdn -->
    <link rel="stylesheet"
          href="https://cdn.datatables.net/1.11.3/css/dataTables.bootstrap5.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/BudgetPlanningWebApplication.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                @*@if (Context.Request.Cookies.ContainsKey("uId"))
                {
                    <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">На главную</a>
                }*@
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            @*@if (Context.Request.Cookies.ContainsKey("uId"))
                            {
                                <a class="nav-link text-dark" asp-area="" asp-controller="RT" asp-action="LogOut">Выход</a>
                                <a class="nav-link text-dark" asp-area="" asp-controller="Users" asp-action="Details" asp-route-id="@Convert.ToInt32(Context.Request.Cookies["uId"])">Профиль</a>
                            }*@
                            @*else
                            {
                                <a class="nav-link text-dark" asp-area="" asp-controller="RT" asp-action="Index">Авторизация</a>
                            }*@
                        </li>
                        <li class="nav-item">
                            @if (Context.Request.Cookies.ContainsKey("uId"))
                            {
                                
                            }
                            @*else
                            {
                                <a class="nav-link text-dark" asp-area="" asp-controller="RT" asp-action="Registr">Регистрация</a>
                            }*@
                            
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    
    
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
